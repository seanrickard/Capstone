
@{
    ViewData["Title"] = "CategoryManagement";
}

<h2>Category Management</h2>

@*@if (!Model.Any())
{
    <tr><td colspan="3" class="text-center">No departments yet</td></tr>
}
else
{*@
    <table class="table table-condensed table-bordered" header header-content="Department Management">
        <tr>
            <th>Name</th>
            <th>Active</th>
        </tr>

        @*@foreach (Department department in Model)
        {

            <tr>
                <td>@department.Id</td>
                <td>@department.DepartmentName</td>
                <td>@department.Active</td>
                <td>@department.Division.DivisionName</td>

                <td>
                    <form asp-action="DeleteDepartment" asp-route-id="@department.Id" method="post">
                        <a class="btn btn-sm btn-primary" asp-action="EditCategory" asp-route-id="@department.Id">Edit</a>




                        <button type="submit" onclick="return confirm('Do you really want to delete this division?');" class="btn btn-sm btn-danger">
                            Delete
                        </button>
                    </form>
                </td>
            </tr>
        }*@

    </table>
@*}*@
<p>
    <a anchor-style="primary" asp-action="AddCategory">Add new category</a>
</p>

