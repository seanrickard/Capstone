@model IEnumerable<PurchaseReq.MVC.Components.PurchaseDeptMenuItem>

<li class="dropdown">
    <a asp-controller="Admin"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Menu<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var menuItem in Model)
        {
            <li>
                <a asp-controller="Admin" asp-action=@menuItem.ActionValue>@menuItem.DisplayValue</a>
            </li>
        }
    </ul>
</li>
